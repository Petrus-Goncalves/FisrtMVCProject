@model LoginModel

@{
    Layout = "_LayoutVazio";
    ViewData["Title"] = "Login";
}

<form asp-controller="Login" asp-action="Login" method="post">


    @if (TempData["ErrorLoginEmailMessage"] != null)
    {
        <div class="alert alert-danger" role="alert">
            <button type="button" class="btn btn-danger btn-sm close-alert" aria-label="Close">X</button>
            @TempData["ErrorLoginEmailMessage"]
        </div>
    }
    @if (TempData["ErrorLoginPasswordMessage"] != null)
    {
        <div class="alert alert-danger" role="alert">
            <button type="button" class="btn btn-danger btn-sm close-alert" aria-label="Close">X</button>
            @TempData["ErrorLoginPasswordMessage"]
        </div>
    }
    @if (TempData["ErrorInvalidLoginMessage"] != null)
    {
        <div class="alert alert-danger" role="alert">
            <button type="button" class="btn btn-danger btn-sm close-alert" aria-label="Close">X</button>
            @TempData["ErrorInvalidLoginMessage"]
        </div>
    }

    <div class="text-center">
        <br />
        <h1>Login</h1>
        <br />

        <div class="form-group">
            <label class="form-label d-grid gap-2 d-md-flex justify-content-md-start">Login</label>
            <input asp-for="Email" type="text" class="form-control" placeholder="Exemplo: josedasilva@hotmail.com" />
        </div>
        <br />
        <div class="form-group">
            <label class="form-label d-grid gap-2 d-md-flex justify-content-md-start">Senha</label>
            <input asp-for="Senha" type="password" class="form-control" placeholder="Exemplo: jose123456" />
        </div>

        <br />
        <button class="btn btn-primary" type="submit">Login</button>
    </div>
</form>